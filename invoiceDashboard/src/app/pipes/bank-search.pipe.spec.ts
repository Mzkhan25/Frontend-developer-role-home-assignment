import { BankSearchPipe } from './bank-search.pipe';

describe('BankSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BankSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
